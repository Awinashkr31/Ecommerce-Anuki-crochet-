import CustomClient from "./CustomClient";

export const metadata = {
  title: 'Custom Design Request - Anuki Crochet',
  description: 'Request a bespoke, custom-made crochet creation crafted just for you.',
};

export default function CustomPage() {
  return <CustomClient />;
}
